# CurrencyExchangeBot
Telegram‑бот для конвертации валют на Python. Использует публичный API для получения актуальных курсов https://api.exchangerate-api.com/v4/latest/<BASE> (вместо <BASE> подставляется базовая валюта).
Команды, который выполняет Telegram-бот:
- '/start' или '/helpMe' — показывает инструкцию по использованию,
- '/values' — выводит список доступных валют,
- конвертация валют происходит при вводе сообщения в формате '<валюта1> <валюта2> <количество>. Пример, доллар евро 100

Файлы проекта:
app.py, config.py, exceptions.py

Бот работает в режиме long polling и обрабатывает входящие обновления, пока процесс не будет остановлен.

Линка на бот: https://t.me/CurrencyX11Bot (username: CurrencyX11Bot)
